# Generic base image (ordomatics/odoo). Client forks (main/dev branches of
# this template) add their own deps on top for their own custom addons.
#
# pyOpenSSL: needed by Odoo core itself, not any addon here -- base's own
# ir_mail_server.py does `import OpenSSL`. Ships with the base odoo image
# at whatever version it was built with; other deps below (mcp, etc.) pull
# in cryptography unpinned, resolving to latest at build time. An old
# pyOpenSSL against a too-new cryptography breaks with "module 'lib' has
# no attribute 'GEN_EMAIL'" -- confirmed live 2026-08-08 (setup-modules-only
# crash-looping on a freshly seeded, freshly onboarded client, same bug
# already fixed in ordomatics/odoo's own requirements.txt but never
# ported to this generic base image branch).
pyOpenSSL>=24.2.1
requests>=2.25.0
# llm_thread
emoji
markdown2
# llm_tool, llm_mcp_server
pydantic>=2.0.0
mcp
# llm_mssql (needs Microsoft ODBC Driver 18 at the OS level too — see Dockerfile)
pyodbc
# llm_assistant (jinja2/pyyaml are already present in odoo:17.0/18.0's base
# Python env; jsonschema confirmed missing on both 2026-08-07 — the only
# one actually needed here; added here too for consistency across versions)
jsonschema
